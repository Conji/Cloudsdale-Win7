<Page x:Class="CloudsdaleWin7.CloudView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:MVVM="clr-namespace:CloudsdaleWin7.MVVM" xmlns:Controls="clr-namespace:CloudsdaleWin7.Controls"
      mc:Ignorable="d" 
      d:DesignHeight="564.045" d:DesignWidth="819.101" Foreground="White"
	Title="CloudView">

    <Page.Resources>
        <MVVM:StatusConverter x:Key="StatusConverter"/>
        <MVVM:DependencyJToken x:Key="Cloud" />
    </Page.Resources>

    <Grid Background="#FFFAFAFA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130" />
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal" CanHorizontallyScroll="True" ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Disabled" ScrollViewer.CanContentScroll="True" Grid.ColumnSpan="2" Height="37" VerticalAlignment="Top">
            <StackPanel.Background>
                <SolidColorBrush Color="{DynamicResource PrimaryBlue}"/>
            </StackPanel.Background>
            <StackPanel Width="434">
            </StackPanel>
            <Rectangle Fill="#FFF4F4F5" Stroke="Black" Width="360" Margin="20,57,20,62" Visibility="Hidden"/>
        </StackPanel>
        <ScrollViewer x:Name="ChatScroll" Grid.ColumnSpan="2" Grid.Row="1">
            <ItemsControl x:Name="ChatMessages">
                <ItemsControl.Background>
                    <SolidColorBrush Color="{DynamicResource PrimaryBackground}"/>
                </ItemsControl.Background>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Separator VerticalAlignment="Top" Opacity=".5" />
                            <Grid Visibility="{Binding [orgcontent], ConverterParameter=Inverse, Converter={StaticResource SlashMe}}"
                                  Margin="4,6,4,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Margin="3">
                                    <Image Source="{Binding [author][avatar][preview], Converter={StaticResource StringUriConv}}" 
                                           Width="60" Height="60"
                                           Cursor="Hand"/>
                                    <Border Background="{Binding [author][role], Converter={StaticResource TagColor}}"
                                            Visibility="{Binding [author][role], Converter={StaticResource HCT}}"
                                            Width="60" Margin="0,1" CornerRadius="4" BorderThickness="1">
                                        <Border.BorderBrush>
                                            <SolidColorBrush Color="{DynamicResource PrimaryContent}"/>
                                        </Border.BorderBrush>
                                        <TextBlock Text="{Binding [author][role], Converter={StaticResource RoleTag}}" 
                                                   FontSize="12" FontWeight="Bold"  Foreground="White" HorizontalAlignment="Center" />
                                    </Border>
                                </StackPanel>
                                <TextBlock FontFamily="Segoe UI Symbol"  Margin="5" FontSize="15" Cursor="Hand" MouseLeftButtonDown="ShowUserInfo" Grid.Column="1" VerticalAlignment="Top" Foreground="DarkSlateGray"><Run Text="{Binding [author][name], Converter={StaticResource JTSC}}" Foreground="{Binding [author], ConverterParameter={StaticResource Cloud}, Converter={StaticResource NameColor}}" /><Run Text=" (@"/><Run Text="{Binding [author][username], Converter={StaticResource JTSC}}" Foreground="{Binding [author][status], Converter={StaticResource StatusConverter}}"/><Run Text=")" DataContext="{Binding [author]}"/><LineBreak/><InlineUIContainer>   
                                    <ItemsControl x:Name="MessageContent" ItemsSource="{Binding [content], Converter={StaticResource MessageControl}}">
                                        
                                    	<ItemsControl.ContextMenu>
                                    		<ContextMenu>
                                                <MenuItem Header="Quote..." DataContext="{Binding [content]}" Click="Quote"/>
                                                <MenuItem Header="Copy all text..."/>
                                                <Separator/>
                                                <MenuItem Header="View user info"/>
                                            </ContextMenu>
                                    	</ItemsControl.ContextMenu>
                                            <ItemsControl.ItemTemplate> 
                                                <DataTemplate>
                                                    <Grid>
                                                        <Controls:LinkDetectingTextBlock TextWrapping="Wrap"
                                                                   Foreground="{Binding Converter={StaticResource ChatColor}}" LinkedText="{Binding}"/>
                                                    </Grid>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </InlineUIContainer></TextBlock>
                                <StackPanel Grid.Column="2">
                                    <TextBlock FontSize="16" HorizontalAlignment="Right" Foreground="Black"><Run Text="{Binding [timestamp], Converter={StaticResource TDTS}}" /><Run Text=" "/><Run Text="{Binding [device], Converter={StaticResource DeviceIcon}}" /></TextBlock>
                                    <ItemsControl ItemsSource="{Binding [drops]}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel MaxWidth="500" />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border BorderBrush="DarkGray" BorderThickness="3" Margin="10">
                                                    <Image Width="120" Height="90" Cursor="Hand" MouseUp="DropUp"
                                                       Source="{Binding [preview], Converter={StaticResource StringUriConv}}" />
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Grid>
                            <StackPanel Visibility="{Binding [orgcontent], Converter={StaticResource SlashMe}}" Margin="5">
                                <Grid>
                                    <TextBlock Text="{Binding [timestamp], Converter={StaticResource TDTS}}" 
                                               FontSize="14" HorizontalAlignment="Center" Foreground="Black" />
                                    <TextBlock Text="{Binding [device], Converter={StaticResource DeviceIcon}}"
                                               FontSize="14" HorizontalAlignment="Right" Foreground="Black" />
                                </Grid>
                                <TextBlock Text="{Binding [content], Converter={StaticResource JTSC}}" 
                                           FontSize="18" HorizontalAlignment="Center" Margin="0,0,0,10" Foreground="Black"
                                           TextWrapping="Wrap" FontFamily="Segoe UI">
                                    <TextBlock.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="Quote..." DataContext="{Binding [content]}" Click="Quote"/>
                                            <MenuItem Header="Copy all text..."/>
                                            <Separator/>
                                            <MenuItem Header="View user info"/>
                                        </ContextMenu>
                                    </TextBlock.ContextMenu>
                                </TextBlock>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <Controls:LinkDetectingTextBlock TextWrapping="Wrap" Text="LinkDetectingTextBlock"/>
            </ItemsControl>
        </ScrollViewer>
        <TextBox Grid.Row="2" x:Name="InputBox" FontSize="18" KeyDown="SendBoxEnter" Grid.ColumnSpan="2" SpellCheck.IsEnabled="True" TextChanged="CheckIfTextIsMultiLine" Padding="27,0,0,0" >
            
        </TextBox>
        <TextBlock VerticalAlignment="Top" FontSize="16" Margin="10,10,10,0" Text="{Binding [name]}" Height="27" FontFamily="Segoe UI Semibold" Grid.ColumnSpan="2"/>
        <Button Content="&#xE15F;" HorizontalAlignment="Left" Grid.Row="2" Width="25" BorderBrush="{x:Null}" Background="{x:Null}" FontFamily="Segoe UI Symbol" Click="Button_Click_1"/>
        <Border x:Name="SymbolBox" BorderBrush="Black" BorderThickness="1" HorizontalAlignment="Left" Margin="10,0,0,10" Grid.Row="1" Width="120" Height="131" VerticalAlignment="Bottom" Visibility="Hidden">
            <WrapPanel Margin="-1">
                <WrapPanel.Background>
                    <SolidColorBrush Color="{DynamicResource PrimaryContent}"/>
                </WrapPanel.Background>
                <TextBlock Width="20" Height ="20" Margin="4,4,4,4" Text="™"/>
                <TextBlock Width="20" Height ="20" Margin="4,4,4,4" Text="😄" ToolTip=":)"/>
                <TextBlock Width="20" Height ="20" Margin="4,4,4,4" Text="😃" ToolTip=":D"/>
                <TextBlock Width="20" Height ="20" Margin="4,4,4,4" Text="😠" ToolTip=">:("/>
                <TextBlock Width="20" Height ="20" Margin="4,4,4,4" Text="😋" ToolTip=":P"/>
                <TextBlock Width="20" Height ="20" Margin="4,4,4,4" Text="😉" ToolTip=";)"/>
                <TextBlock Width="20" Height ="20" Margin="4,4,4,4" Text="😒" ToolTip="-.-"/>
                <TextBlock Width="20" Height ="20" Margin="4,4,4,4" Text=""/>
                <TextBlock Width="20" Height ="20" Margin="4,4,4,4" Text=""/>
                <TextBlock Width="20" Height ="20" Margin="4,4,4,4" Text=""/>
                <TextBlock Width="20" Height ="20" Margin="4,4,4,4" Text=""/>
                <TextBlock Width="20" Height ="20" Margin="4,4,4,4" Text=""/>
                <TextBlock Width="20" Height ="20" Margin="4,4,4,4" Text=""/>
                <TextBlock Width="20" Height ="20" Margin="4,4,4,4" Text=""/>
                <TextBlock Width="20" Height ="20" Margin="4,4,4,4" Text=""/>
                <TextBlock Width="20" Height ="20" Margin="4,4,4,4" Text=""/>
                <TextBlock Width="20" Height ="20" Margin="4,4,4,4" Text=""/>
            </WrapPanel>
        </Border>
    </Grid>
</Page>
